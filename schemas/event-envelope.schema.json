{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contynu.com/schemas/event-envelope.schema.json",
  "title": "Contynu Event Envelope",
  "type": "object",
  "required": [
    "schema_version",
    "event_id",
    "session_id",
    "seq",
    "ts",
    "actor",
    "event_type",
    "payload_version",
    "payload",
    "checksum"
  ],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 1 },
    "event_id": { "type": "string", "pattern": "^evt_[a-f0-9]{32}$" },
    "session_id": { "type": "string", "pattern": "^ses_[a-f0-9]{32}$" },
    "turn_id": { "type": ["string", "null"], "pattern": "^trn_[a-f0-9]{32}$" },
    "seq": { "type": "integer", "minimum": 1 },
    "ts": { "type": "string", "format": "date-time" },
    "actor": {
      "type": "string",
      "enum": ["user", "assistant", "tool", "system", "runtime", "filesystem", "adapter"]
    },
    "event_type": { "type": "string", "minLength": 1 },
    "payload_version": { "type": "integer", "minimum": 1 },
    "parent_event_id": { "type": ["string", "null"], "pattern": "^evt_[a-f0-9]{32}$" },
    "correlation_id": { "type": ["string", "null"] },
    "causation_id": { "type": ["string", "null"], "pattern": "^evt_[a-f0-9]{32}$" },
    "tags": {
      "type": "array",
      "items": { "type": "string" }
    },
    "payload": { "type": "object" },
    "checksum": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" }
  },
  "additionalProperties": false
}
